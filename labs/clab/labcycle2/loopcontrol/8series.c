#include<stdio.h>
main()
{
  int x,n,term=1,sum=0,k=1,i;
  printf("Enter Number ");
  scanf("%d",&x);
  printf("Enter term: ");
  scanf("%d",&n)
  for(i=0 ; i<n; i++)
   {
     sum=sum+term;
     term = (term * (-x*x));
   }
  printf("n",);
}

